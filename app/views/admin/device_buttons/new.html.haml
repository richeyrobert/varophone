/ TODO: later i need to get rid of the sidebar and deploy this as a modal

= render 'layouts/phone_admin_sidebar'
%h3 New Device Buttons for:
%h1= @pbx.name

= render 'form'

= link_to 'Back', edit_admin_device_path(@device)
